Customer visited the Techjockey Homepage.
<% if (typeof childCategory !== 'undefined' && childCategory) { %>Customer searched for <%= childCategory %>.<% } %>
<% if (typeof primaryCategory !== 'undefined' && primaryCategory) { %>Customer explored products in <%= primaryCategory %> category.<% } %>
Customer reviewed pricing options.
<% if (typeof vendorProduct !== 'undefined' && vendorProduct) { %>Customer requested a Demo for <%= vendorProduct %>.<% } %>
