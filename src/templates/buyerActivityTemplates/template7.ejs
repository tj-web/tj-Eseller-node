Customer visited the Techjockey Homepage.
<% if (typeof primaryCategory !== 'undefined' && primaryCategory) { %>Customer searched for <%= primaryCategory %>.<% } %>
<% if (typeof childCategory !== 'undefined' && childCategory) { %>Customer explored products in <%= childCategory %> category.<% } %>
Customer viewed product features.
Customer reviewed pricing options.
Customer viewed customer reviews.
Customer compared shortlisted solutions.
<% if (typeof vendorProduct !== 'undefined' && vendorProduct) { %>Customer requested a Demo for <%= vendorProduct %>.<% } %>
