<% if (typeof primaryCategory !== 'undefined' && primaryCategory) { %>Customer searched for <%= primaryCategory %>.<% } %>
<% if (typeof childCategory !== 'undefined' && childCategory) { %>Customer explored features of <%= childCategory %>.<% } %>
Customer viewed multiple product pages.
Customer reviewed pricing options.
Customer downloaded a brochure.
<% if (typeof vendorProduct !== 'undefined' && vendorProduct) { %>Customer contacted the seller for <%= vendorProduct %>.<% } %>
